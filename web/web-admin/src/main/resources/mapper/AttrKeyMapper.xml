<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caicode.lease.web.admin.mapper.AttrKeyMapper">



    <select id="listAttrInfo" resultType="com.caicode.lease.web.admin.vo.attr.AttrKeyVo">
        SELECT
            k.id AS key_id,
            k.name AS key_name,
            v.id AS attr_value_id,
            v.name AS attr_value_name,
            v.attr_key_id
        FROM
            attr_key k
                JOIN
            attr_value v
            ON
                k.id = v.attr_key_id
        WHERE
            k.is_deleted = 0
          AND v.is_deleted= 0;
    </select>

</mapper>
